# 2026年全国大学生计算机系统能力大赛-操作系统设计赛(全国)-OS功能挑战赛道 proj9 进度

## 2026.04.23 已合入主线

### 支持 `/proc/[pid]/maps`，打印用户空间映射信息

PR [#2725](https://github.com/asterinas/asterinas/pull/2725) Add `/proc/[pid]/maps`

 - d44c3750f (wsy/proc_pid_maps) Add `/proc/[pid]/maps`

PR [#2925](https://github.com/asterinas/asterinas/pull/2925) Add entries other than `[stack]` in `/proc/[pid]/maps`

 - cfce5e764 Revert "`ProgramToLoad` only needs a `elf_inode: Arc<dyn Inode>` instead of a `Path`"
 - e364c705f (wsy/maps) Add entries other than `[stack]` in `/proc/[pid]/maps`

### 支持 `/proc/[pid]/mem`，读写其他用户空间内存

PR [#2449](https://github.com/asterinas/asterinas/pull/2449) Support accessing user spaces of other processes

 - b7cf1b8ba Support accessing user spaces of other processes
 - 9901dc85f Support reading `InitStack` across pages
 - 18cb1903c Support `/proc/[pid]/mem` and `/proc/[pid]/task/[tid]/mem`
 - 2c83f85db (wsy/access_remote_vmspace) Add a regression test for `/proc/[pid]/mem`

PR [#2855](https://github.com/asterinas/asterinas/pull/2855) Support force-write via `/proc/pid/mem`

 - bee9c37b3 Add `is_forced` into `PageFaultInfo`
 - 50addd046 Support force-write via `/proc/pid/mem`
 - fe3cf3ad7 Add ptrace access mode checks
 - 658d4507b (wsy/force_remote_access) Rename `Vmar::access_remote` to `Vmar::access_alien`

### 支持 Yama LSM，限制 ptrace 安全边界

PR [#3018](https://github.com/asterinas/asterinas/pull/3018) Add Yama ptrace scope

 - 234831832 Add `/proc/sys/kernel/yama/ptrace_scope`
 - 366ccced4 (wsy/yama_ptrace_scope) Add regression tests for Yama ptrace scope

### 支持 `/proc/pid/auxv`，打印进程的 auxiliary vector

PR [#3073](https://github.com/asterinas/asterinas/pull/3073) Add `/proc/pid/auxv`

 - 8f7037aec (wsy/auxv, auxv) Add `/proc/pid/auxv`

### 支持每个线程的 `/proc/[tid]` 文件夹

PR [#3125](https://github.com/asterinas/asterinas/pull/3125) Add `/proc/[tid]`

 - 602684036 Add `/proc/[tid]`
 - a9aff5a36 (wsy/proc_tid, proc_tid) Add regression tests for `/proc/[tid]`

### 支持 `tkill` 系统调用

PR [#3150](https://github.com/asterinas/asterinas/pull/3150) Add `tkill` syscall

 - 47d025d9e Add `tkill` syscall
 - 12214beab (wsy/tkill, tkill) Add scml for `tkill`

## 2026.04.26 已合入主线

### 支持 `ptrace` 系统调用框架

PR [#2984](https://github.com/asterinas/asterinas/pull/2984) Add `ptrace` syscall

本 PR 引入了基础的 ptrace stop / wait / continue 支持，涵盖 ptrace 的 PTRACE_TRACEME、PTRACE_CONT（包括注入 signal 功能），并确保其与 wait、exit、execve 等 syscall 能够正确协同工作。

 - e534e9937 Add `ptrace(PTRACE_TRACEME)`
 - 754191ddf Carry signal origin information in `DequeuedSignal`
 - ff487900d Defer `SA_RESETHAND` reset until signal handling
 - 7d78eb3d5 Support ptrace-stop
 - f730df0c1 Store `ExitCode` for each `PosixThread`
 - 948c11810 Support `wait`ing for ptrace-stops
 - 04732c255 Add `ptrace(PTRACE_CONT)`
 - f1eb095fe Send `SIGTRAP` on successful `execve` for tracees
 - 7046913c3 Add SCML for ptrace
 - 6f7fbed7e (wsy/add_ptrace, add_ptrace) Add basic ptrace tests

## 2026.05.14 已合入主线

### 支持使用 `ptrace` 调试

PR [#3065](https://github.com/asterinas/asterinas/pull/3065) Support debugging with `ptrace`

本 PR 引入了使用 ptrace 读写 tracee 用户寄存器的能力，涵盖 PTRACE_GETREGS, PTRACE_SETREGS, PTRACE_PEEKUSER, PTRACE_POKEUSER，并基于此支持单步执行（PTRACE_SINGLESTEP）。同时，支持了额外的 PTRACE_GETSIGINFO 和 PTRACE_KILL。本 PR 合入后，主线可以支持正确运行一个最小化的调试器用户程序。

 - e928dbd7c Store `orig_syscall_ret` in `PosixThread`
 - c8dac736a Move x86_64 into workspace dependencies
 - 9eab8beff Support reading and writing user general regs via `ptrace`
 - f6d4c8c8d Add `ptrace(PTRACE_SINGLESTEP)` and breakpoint support
 - 01fa22027 Add `ptrace(PTRACE_GETSIGINFO)` and `ptrace(PTRACE_KILL)`
 - 6d65f0909 Update SCML for `ptrace`
 - 1852eb41d Add some tests for `ptrace`
 - cad103f23 Setting invalid FS base with `arch_prctl` should fail with `EPERM`
 - d1409d7f9 Revise docs for `PtraceRequest`

## 2026.05.18 已合入主线

### 支持 `ptrace(PTRACE_SETOPTIONS)` 和 ptrace-event-stop

PR [#3061](https://github.com/asterinas/asterinas/pull/3061) Support `PTRACE_SETOPTIONS`

本 PR 引入了 `PTRACE_SETOPTIONS`，以及相应的在 exec, exit 时按 options 发生 ptrace-event-stop 的能力。初期目标是支持 GDB 对单线程程序的调试，因此本 PR 尚未实现 clone-family 的 ptrace-event-stops。在本 PR 中，会触发 ptrace-event-stops 的 clone 调用将被拒绝。

 - 946f60bc8 Add `ptrace(PTRACE_SETOPTIONS)` and `ptrace(PTRACE_GETEVENTMSG)`
 - 10e80678e Support ptrace option `EXITKILL`
 - 70949d52c Support ptrace options: EXEC, EXIT
 - 01b595b41 Fix `Context` visibility for Rust compiler
 - 953c3cd3b Reject unsupported `clone` calls under ptrace
 - 38cd184bd Update SCML for `ptrace`
 - 26f8a8a8d (wsy/ptrace_setoptions, ptrace_setoptions) Add some tests for ptrace options

## 2026.05.21 已合入主线

### 使 x86 下 USER_CS 和 USER_SS 与 Linux 对齐

PR [#3243](https://github.com/asterinas/asterinas/pull/3243) Add unused KCODE32 slot in x86 GDT to align USER_CS and USER_SS with Linux

通过在 GDT 里给 UCODE64 和 UDATA 前面插入一个空描述符 KCODE32，把 CS 从 0x2b 调整成 Linux 上 GDB 期望的 0x33（同时 SS=0x2b），从而消除 GDB 警告。

 - 82011dd66 Add unused KCODE32 slot in GDT to align USER_CS and USER_
SS with Linux

### 为 GDB 添加内核侧补丁、经验证的使用文档和持续集成测试

PR [#3254](https://github.com/asterinas/asterinas/pull/3254) Add (kernel-side) patches, verified usage docs, and CI for GDB

本 PR 将此前依赖应用侧补丁的 GDB 兼容逻辑迁移至内核侧，以避免因 GDB 被大量且体积庞大的包依赖而带来的重编译成本。实现上，本 PR 初步模拟了读 x86 debug registers 和部分 personality 行为。本 PR 合入后，主线能够直接支持 GDB 对单线程程序的调试。同时补充了 GDB 的使用文档与 CI。

 - 74e0bb062 Dummy support reading x86 debug registers
 - 15ec6f6a8 Dummy support `personality(ADDR_NO_RANDOMIZE)`
 - 5788ea591 Add verified usage docs and CI for GDB

## 2026.05.28 已合入主线

### 支持 `ptrace(PTRACE_SYSCALL)` 和 ptrace-syscall-stop

PR [#3241](https://github.com/asterinas/asterinas/pull/3241) Add `ptrace(PTRACE_SYSCALL)`

支持在 tracer 使用 PTRACE_SYSCALL 以继续 tracee 执行时，tracee 可以在 syscall entry/exit 进行ptrace-syscall-stop. 同步支持 `PTRACE_O_TRACESYSGOOD`.

 - cd08126de Store `PtraceWaitStatus` in `StopDeliverySignal::Pending`
 - 9e95d0be5 Add `ptrace(PTRACE_SYSCALL)`
 - 10c33ff88 Add a regression test for `PTRACE_SYSCALL`
 - cbf8ece9b Update `ptrace` SCML for `PTRACE_SYSCALL`

### 支持 `ptrace(PTRACE_PEEK/POKE_TEXT/DATA)` 以读写 tracee 用户空间

PR [#3242](https://github.com/asterinas/asterinas/pull/3242) Add `ptrace(PTRACE_PEEK/POKE_TEXT/DATA)`

支持 tracer 使用这一系列 requests 读写 tracee 用户空间的内存，提供和 `/proc/[pid]/mem` 一致的接口。本 PR 合入后，主线能够直接支持 GDB 对单线程程序的调试。

 - 030597950 Add `ptrace(PTRACE_PEEK/POKE_TEXT/DATA)`
 - 6387d0bd2 Add a regression test for `PTRACE_PEEK/POKE_TEXT/DATA`
 - 704350aa0 Update `ptrace` SCML for `PTRACE_PEEK/POKE_TEXT/DATA`

### 为 `strace` 添加经验证的使用文档和持续集成测试

PR [#3266](https://github.com/asterinas/asterinas/pull/3266) Add CI and verified usage docs for `strace`

本 PR 补充了 strace 的使用文档与 CI。

 - 3cde7a6fe Add CI and verified usage docs for `strace`
